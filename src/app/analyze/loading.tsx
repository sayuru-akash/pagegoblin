import { CaveLoader } from "@/components/feedback/cave-loader";

export default function AnalyzeLoading() {
  return (
    <CaveLoader
      title="I caught the scent."
      detail="I’m crawling toward the page now. Keep the cave door open."
    />
  );
}
